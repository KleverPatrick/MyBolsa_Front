import { Binary } from '@angular/compiler';


export interface Producto {
    id: number;
    name: string;
    description: string;
    precio: string;
    imageUrl: string;
    productCode: string;
}